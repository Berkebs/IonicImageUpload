import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.berkeburaksarp.ionicupload',
  appName: 'imgup',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
